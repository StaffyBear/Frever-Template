// Database access intentionally remains disconnected in template v0.1.
// Future app-specific repositories should call Supabase through this module
// rather than placing database queries directly inside page files.

export const Database = {
  isEnabled: false
};
